import React from "react"
import Form from "./Form"

const PasswordRequestRequest = (props) => {
  return <Form fields={["Registered Email Address"]} buttonName="Reset My Password" />
};

export default PasswordRequestRequest;
