import { React } from "react";

import Difficulty from "./Difficulty";
import Status from "./Status";
import Tags from "./Tags";

const FilterButton = ({ filterName, isDifficultyActive, isStatusActive, isTagsActive, handleClick, addTag }) => {
  if (filterName === "Difficulty") return 
  if (filterName === "Status") return 
  if (filterName === "Tags") return 
};

export default FilterButton;
